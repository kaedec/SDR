# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Spectrum Analyzer gnuradio-companion Output Files #
############
# These files are very large and just contain raw data
# not to be committed to the repository

spectrum_analyzer/*.bin
